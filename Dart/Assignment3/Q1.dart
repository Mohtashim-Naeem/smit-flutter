// Q.1: Create a list of names and print all names using the List method.
void main() {
  List names = ["zsad", "basit", "naeem", "hassan", "ahsan"];

  // print(names.elementAt());
  names.forEach((name) {
    print(name);
  });
  // names.sort();
  // print(names);

  // names = List.of(names.reversed);
  // print(names);
}
